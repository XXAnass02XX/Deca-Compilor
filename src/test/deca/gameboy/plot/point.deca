#include "GameBoy.java"

{
    boolean updated = false;
    int a = 1;
    GameBoy gb = new GameBoy();
    gb.init();
    gb.setBackgroundColor(gb.DARK);
    gb.setColor(gb.LIGHT, 15, 5);
    while (!updated) {
        updated = gb.updateScreen();
    }
    //gb.sleep(1);
    //gb.setColor(gb.DARK, 15, 5);
    //gb.setColor(gb.LIGHT, 6, 5);
    //gb.sleep(1);
    //gb.setColor(gb.DARK, 6, 5);
    //gb.setColor(gb.LIGHT, 7, 5);
    //gb.sleep(1);
    //while (!updated) {
    //    updated = gb.updateScreen();
    //}
}