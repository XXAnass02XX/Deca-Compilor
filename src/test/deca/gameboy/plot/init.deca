#include "GameBoy.java"

{
    //boolean updated = false;
    GameBoy gb = new GameBoy();
    gb.init();
    gb.setBackgroundColor(gb.DARK);
    //gb.setTile(125, 1, 1);
    while (true) {
        gb.updateScreen();
        //gb.sleep(2);
        //i = i + 1;
    }
}