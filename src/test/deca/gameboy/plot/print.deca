#include "GameBoy.java"

{
    GameBoy gb = new GameBoy();
    int x;
    gb.init();
    //gb.setBackgroundColor(gb.DARK);
    //println();
    //print("test de depassement de l ecran j espere ca marche",2,2);
    x = 12;
    gb.printNumber(x, 2, 1);
    print("score ", 2, 3);
}
